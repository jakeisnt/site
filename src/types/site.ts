type PageSettings = {
  rootUrl: string;
  siteName: string;
  sourceDir: string;
  targetDir: string;
  resourcesDir: string;
  faviconsDir: string;
  fallbackSourceDir?: string;
  ignorePaths?: string[];
};

export type { PageSettings };
